# Heavy numerical packages on ARM come from Kali's mutually compatible binary
# package set. Keep this file for pip's ARM constraint hook, but do not force a
# NumPy generation that may differ from the current Kali release.
